<p id="notice"><%= notice %></p>


  <h1 style = justify-center > <strong> The composition of a <%= @cocktail.name.capitalize %></strong> </h1>
  </br>
  <% if @cocktail.photo? %>
  <%= cl_image_tag @cocktail.photo.path, width: 200, height: 300, crop: :fill %>
<% end %>

  <% @cocktail.doses.each do |dose|%>
   <p> - <%= dose.description %> of
    <%= dose.ingredient.name %> |
    <%= link_to 'Delete', dose_path(dose.id), method: :delete, data: { confirm: 'Are you sure?' } %> </p>
  <%end%>

</br>
</br>

<p> Actions :
<%= link_to 'Add a new ingredient and its dose',new_cocktail_dose_path(@cocktail.id) %> |
<%= link_to 'Go back to the list', cocktails_path %> </p>

